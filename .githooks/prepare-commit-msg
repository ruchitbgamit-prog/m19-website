#!/bin/sh
# Strip AI co-author / Made-with trailers so commits show only your Git identity.
if [ -f "$1" ]; then
  grep -v -E '^(Co-authored-by: Cursor|Made-with: Cursor|Made with Cursor)' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
